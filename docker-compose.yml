services:
  search:
    build: .
    working_dir: /app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    command: uv run streamlit run app.py --server.address=0.0.0.0 --server.port=8501